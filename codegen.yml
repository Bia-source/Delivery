overwrite: true
schema: "http://localhost:4000"
generates:
  src/generated/schemas.ts:
    plugins:
      - "typescript"
      - "typescript-resolvers"
  ./graphql.schema.json:
    plugins:
      - "introspection"
